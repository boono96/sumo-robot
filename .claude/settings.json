{
  "hooks": {
    "Stop": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "shell": "bash",
            "command": "cd \"d:/usefile/code/vibeCode/claude/Project1\" && git add -A && (git diff --cached --quiet || (git commit -m \"auto: snapshot $(date -u +%Y-%m-%dT%H:%M:%SZ)\" && git push origin main))",
            "timeout": 60,
            "statusMessage": "auto-commit + push to GitHub"
          }
        ]
      }
    ]
  }
}
